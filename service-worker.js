const CACHE='pill-return-v15-pillens-count-only';
const ASSETS=['./','./index.html','./manifest.webmanifest','./icon-192.png','./icon-512.png'];
self.addEventListener('install',e=>e.waitUntil(caches.open(CACHE).then(c=>c.addAll(ASSETS)).then(()=>self.skipWaiting())));
self.addEventListener('activate',e=>e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim())));
function openDB(){return new Promise((resolve,reject)=>{const r=indexedDB.open('pill-return-share-db',1);r.onupgradeneeded=()=>{const db=r.result;if(!db.objectStoreNames.contains('shares'))db.createObjectStore('shares',{keyPath:'id'})};r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error)})}
async function saveSharedFiles(files){const db=await openDB();return new Promise((resolve,reject)=>{const tx=db.transaction('shares','readwrite');tx.objectStore('shares').put({id:'latest',blobs:files,names:files.map(f=>f.name||'pillens.jpg'),savedAt:Date.now()});tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error)})}
self.addEventListener('fetch',event=>{const url=new URL(event.request.url);if(event.request.method==='POST'&&url.pathname.endsWith('/share-target')){event.respondWith((async()=>{try{const form=await event.request.formData();const files=form.getAll('image').filter(f=>f&&typeof f==='object'&&'arrayBuffer' in f);if(files.length)await saveSharedFiles(files)}catch(e){console.error(e)}return Response.redirect(new URL('./?shared=1&v=15',event.request.url).href,303)})());return}if(event.request.method==='GET'){event.respondWith(fetch(event.request).then(r=>{const copy=r.clone();caches.open(CACHE).then(c=>c.put(event.request,copy));return r}).catch(()=>caches.match(event.request).then(x=>x||caches.match('./index.html'))))}});
